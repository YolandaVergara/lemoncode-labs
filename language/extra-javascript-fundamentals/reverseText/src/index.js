//Dado un texto cualquiera, invierte el orden de las palabras.

const reverseText = (string) => {
  return console.log(string.split(' ').reverse());
}

reverseText("Dado un texto cualquiera, invierte el orden de las palabras")